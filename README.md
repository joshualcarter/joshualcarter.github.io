# joshualcarter.github.io

This is the GitHub Pages site for Josh Carter.

## Quick updates
- Edit links in `index.html` under the `LINKS` object.
- Replace resumes in `/docs/` with your real PDFs (keep filenames the same).
- Replace the headshot placeholder in `/assets/` when ready.
